# Inference Engine
High-throughput inference engine with Metal-accelerated compute.

## Usage
1. Install the project using `poetry install`.
2. Run the tests using `pytest`.
3. Use the `InferenceEngine` class to perform inference on input text.
